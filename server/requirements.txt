# EN-learning 后端依赖（M0 脚手架）
# LLM/langchain 相关依赖（langchain-openai 等）由 ai-agent 负责加入，此处不含。

fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
alembic>=1.13
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# LLM（ai-agent：LLMClient 基于 langchain-openai 的 ChatOpenAI 直连智谱 GLM；
# langsmith 供 ChatOpenAI 调用自动上报 LangSmith trace，由环境变量驱动）
langchain-openai==1.6.1
langsmith==0.12.2

# 开发工具
ruff>=0.4
pytest>=8.0
httpx>=0.27
